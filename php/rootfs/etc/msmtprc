defaults
tls ${MSMTP_TLS:-off}

account default
auth ${MSMTP_AUTH:-off}

host ${MSMTP_HOST:-host.docker.internal}
port ${MSMTP_PORT:-1025}

user ${MSMTP_USER:-}
password ${MSMTP_PASS:-}

add_missing_date_header on
